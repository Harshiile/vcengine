import { loginSchema, signupSchema } from "./auth.req";
import { videoSchema, issueSchema, commentSchema } from "./video.req";
import { workspaceSchema, branchSchema } from "./workspace.req";
export {
  loginSchema,
  signupSchema,
  videoSchema,
  issueSchema,
  commentSchema,
  workspaceSchema,
  branchSchema,
};
